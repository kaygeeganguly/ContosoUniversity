diff --git a/.github/modernize/buildmelbourne-modernization-plan/.metadata/tasks.json b/.github/modernize/buildmelbourne-modernization-plan/.metadata/tasks.json
index 6434479..aeda19b 100644
--- a/.github/modernize/buildmelbourne-modernization-plan/.metadata/tasks.json
+++ b/.github/modernize/buildmelbourne-modernization-plan/.metadata/tasks.json
@@ -24,7 +24,13 @@
     {
       "type": "transform",
       "id": "002-transform-msmq-to-servicebus",
-      "status": "started",
+      "status": "success",
+      "summary": "Migrated NotificationService from System.Threading.Channels (interim in-memory) to Azure Service Bus with DefaultAzureCredential (Managed Identity). Added Azure.Messaging.ServiceBus 7.19.0 and Azure.Identity 1.14.2. Queue name ContosoUniversityNotifications preserved. Build succeeds with 0 errors.",
+      "successCriteriaStatus": {
+        "passBuild": "true",
+        "generateNewUnitTests": "false",
+        "passUnitTests": "true"
+      },
       "description": "Migrate Windows MSMQ notification queue to Azure Service Bus",
       "reason": "System.Messaging (MSMQ) is a Windows-only API and is incompatible with Linux containers required by Azure Container Apps. Addresses issue: Windows MSMQ dependency in NotificationService.cs.",
       "requirements": "Migrate NotificationService from System.Messaging.MessageQueue to Azure Service Bus. Use Managed Identity (DefaultAzureCredential) for authentication instead of connection strings. Preserve all existing notification operations: send notification (SendNotification), receive notification (ReceiveNotification), and mark as read (MarkAsRead). Remove the System.Messaging and System.Messaging-based NuGet dependency. The Azure Service Bus queue name should match the existing queue name (ContosoUniversityNotifications).",
@@ -46,6 +52,7 @@
     {
       "type": "transform",
       "id": "003-transform-local-storage-to-blob",
+      "status": "started",
       "description": "Migrate local file system teaching material uploads to Azure Blob Storage",
       "reason": "Local filesystem storage is ephemeral in Azure Container Apps containers. Files uploaded to the local path ~/Uploads/TeachingMaterials/ will be lost on container restarts. Addresses issue: Local filesystem file upload in CoursesController.cs.",
       "requirements": "Migrate teaching material image upload and retrieval in CoursesController from local filesystem (~/Uploads/TeachingMaterials/) to Azure Blob Storage. Use Managed Identity (DefaultAzureCredential) for authentication. Preserve all existing file operations: upload on course create, replace file on course edit (including deletion of old file), delete file on course delete. Preserve existing validation (allowed image extensions: jpg, jpeg, png, gif, bmp; max file size 5MB). Update image URL references to use Azure Blob Storage URLs instead of local virtual paths.",
diff --git a/ContosoUniversity/ContosoUniversity.csproj b/ContosoUniversity/ContosoUniversity.csproj
index c06fca4..08c5c44 100644
--- a/ContosoUniversity/ContosoUniversity.csproj
+++ b/ContosoUniversity/ContosoUniversity.csproj
@@ -11,6 +11,7 @@
   <ItemGroup>
     <PackageReference Include="Azure.Identity" Version="1.14.2" />
     <PackageReference Include="Azure.Messaging.ServiceBus" Version="7.19.0" />
+    <PackageReference Include="Azure.Storage.Blobs" Version="12.28.0" />
     <PackageReference Include="Microsoft.EntityFrameworkCore.SqlServer" Version="10.0.9" />
     <PackageReference Include="Microsoft.EntityFrameworkCore.Tools" Version="10.0.9" />
     <PackageReference Include="Newtonsoft.Json" Version="13.0.3" />
diff --git a/ContosoUniversity/Controllers/CoursesController.cs b/ContosoUniversity/Controllers/CoursesController.cs
index 3d42d79..17cce0f 100644
--- a/ContosoUniversity/Controllers/CoursesController.cs
+++ b/ContosoUniversity/Controllers/CoursesController.cs
@@ -1,6 +1,5 @@
 using Microsoft.AspNetCore.Mvc;
 using Microsoft.EntityFrameworkCore;
-using Microsoft.AspNetCore.Hosting;
 using ContosoUniversity.Data;
 using ContosoUniversity.Models;
 using ContosoUniversity.Services;
@@ -9,12 +8,12 @@ namespace ContosoUniversity.Controllers
 {
     public class CoursesController : BaseController
     {
-        private readonly IWebHostEnvironment _environment;
+        private readonly IBlobStorageService _blobStorageService;
 
-        public CoursesController(SchoolContext context, NotificationService notificationSvc, IWebHostEnvironment environment)
+        public CoursesController(SchoolContext context, NotificationService notificationSvc, IBlobStorageService blobStorageService)
             : base(context, notificationSvc)
         {
-            _environment = environment;
+            _blobStorageService = blobStorageService;
         }
 
         // GET: Courses
@@ -74,21 +73,14 @@ namespace ContosoUniversity.Controllers
 
                     try
                     {
-                        var uploadsPath = Path.Combine(_environment.ContentRootPath, "Uploads", "TeachingMaterials");
-                        if (!Directory.Exists(uploadsPath))
-                        {
-                            Directory.CreateDirectory(uploadsPath);
-                        }
-
                         var fileName = $"course_{course.CourseID}_{Guid.NewGuid()}{fileExtension}";
-                        var filePath = Path.Combine(uploadsPath, fileName);
+                        var contentType = teachingMaterialImage.ContentType;
 
-                        using (var fileStream = new FileStream(filePath, FileMode.Create))
-                        {
-                            await teachingMaterialImage.CopyToAsync(fileStream);
-                        }
+                        using var stream = teachingMaterialImage.OpenReadStream();
+                        var blobUrl = await _blobStorageService.UploadAsync(stream, fileName, contentType);
 
-                        course.TeachingMaterialImagePath = $"~/Uploads/TeachingMaterials/{fileName}";
+                        // Store the full Azure Blob Storage URL (replaces ~/Uploads/TeachingMaterials/ local path)
+                        course.TeachingMaterialImagePath = blobUrl;
                     }
                     catch (Exception ex)
                     {
@@ -154,32 +146,20 @@ namespace ContosoUniversity.Controllers
 
                     try
                     {
-                        var uploadsPath = Path.Combine(_environment.ContentRootPath, "Uploads", "TeachingMaterials");
-                        if (!Directory.Exists(uploadsPath))
+                        // Delete the old blob from Azure Blob Storage before uploading the replacement
+                        if (!string.IsNullOrEmpty(course.TeachingMaterialImagePath))
                         {
-                            Directory.CreateDirectory(uploadsPath);
+                            await _blobStorageService.DeleteAsync(course.TeachingMaterialImagePath);
                         }
 
                         var fileName = $"course_{course.CourseID}_{Guid.NewGuid()}{fileExtension}";
-                        var filePath = Path.Combine(uploadsPath, fileName);
+                        var contentType = teachingMaterialImage.ContentType;
 
-                        // Delete old file if exists
-                        if (!string.IsNullOrEmpty(course.TeachingMaterialImagePath))
-                        {
-                            var oldRelativePath = course.TeachingMaterialImagePath.TrimStart('~', '/').Replace('/', Path.DirectorySeparatorChar);
-                            var oldFilePath = Path.Combine(_environment.ContentRootPath, oldRelativePath);
-                            if (System.IO.File.Exists(oldFilePath))
-                            {
-                                System.IO.File.Delete(oldFilePath);
-                            }
-                        }
+                        using var stream = teachingMaterialImage.OpenReadStream();
+                        var blobUrl = await _blobStorageService.UploadAsync(stream, fileName, contentType);
 
-                        using (var fileStream = new FileStream(filePath, FileMode.Create))
-                        {
-                            await teachingMaterialImage.CopyToAsync(fileStream);
-                        }
-
-                        course.TeachingMaterialImagePath = $"~/Uploads/TeachingMaterials/{fileName}";
+                        // Store the full Azure Blob Storage URL (replaces ~/Uploads/TeachingMaterials/ local path)
+                        course.TeachingMaterialImagePath = blobUrl;
                     }
                     catch (Exception ex)
                     {
@@ -218,7 +198,7 @@ namespace ContosoUniversity.Controllers
         // POST: Courses/Delete/5
         [HttpPost, ActionName("Delete")]
         [ValidateAntiForgeryToken]
-        public IActionResult DeleteConfirmed(int id)
+        public async Task<IActionResult> DeleteConfirmed(int id)
         {
             Course? course = db.Courses.Find(id);
             if (course == null)
@@ -227,20 +207,16 @@ namespace ContosoUniversity.Controllers
             }
             var courseTitle = course.Title;
 
+            // Delete the teaching material blob from Azure Blob Storage before removing the course record
             if (!string.IsNullOrEmpty(course.TeachingMaterialImagePath))
             {
-                var oldRelativePath = course.TeachingMaterialImagePath.TrimStart('~', '/').Replace('/', Path.DirectorySeparatorChar);
-                var filePath = Path.Combine(_environment.ContentRootPath, oldRelativePath);
-                if (System.IO.File.Exists(filePath))
+                try
                 {
-                    try
-                    {
-                        System.IO.File.Delete(filePath);
-                    }
-                    catch (Exception ex)
-                    {
-                        System.Diagnostics.Debug.WriteLine($"Error deleting file: {ex.Message}");
-                    }
+                    await _blobStorageService.DeleteAsync(course.TeachingMaterialImagePath);
+                }
+                catch (Exception ex)
+                {
+                    System.Diagnostics.Debug.WriteLine($"Error deleting blob: {ex.Message}");
                 }
             }
 
@@ -253,3 +229,4 @@ namespace ContosoUniversity.Controllers
         }
     }
 }
+
diff --git a/ContosoUniversity/Program.cs b/ContosoUniversity/Program.cs
index 688b55e..1f9b30f 100644
--- a/ContosoUniversity/Program.cs
+++ b/ContosoUniversity/Program.cs
@@ -1,7 +1,8 @@
+using Azure.Identity;
+using Azure.Storage.Blobs;
 using ContosoUniversity.Data;
 using ContosoUniversity.Services;
 using Microsoft.EntityFrameworkCore;
-using Microsoft.Extensions.FileProviders;
 
 var builder = WebApplication.CreateBuilder(args);
 
@@ -11,6 +12,20 @@ builder.Services.AddDbContext<SchoolContext>(options =>
     options.UseSqlServer(builder.Configuration.GetConnectionString("DefaultConnection")));
 builder.Services.AddSingleton<NotificationService>();
 
+// Register Azure Blob Storage BlobServiceClient as Singleton.
+// BlobServiceClient is thread-safe and designed to be reused  always registered as Singleton.
+// Uses DefaultAzureCredential (Managed Identity) for authentication  no connection strings or keys.
+builder.Services.AddSingleton(sp =>
+{
+    var config = sp.GetRequiredService<IConfiguration>();
+    var serviceUri = new Uri(config["Storage:ServiceUri"]
+        ?? throw new InvalidOperationException("Storage:ServiceUri is not configured."));
+    return new BlobServiceClient(serviceUri, new DefaultAzureCredential());
+});
+
+// Register BlobStorageService as Singleton (holds a BlobContainerClient derived from BlobServiceClient)
+builder.Services.AddSingleton<IBlobStorageService, BlobStorageService>();
+
 var app = builder.Build();
 
 // Initialize database
@@ -31,14 +46,14 @@ app.UseHttpsRedirection();
 // Serve static files from the default wwwroot folder
 app.UseStaticFiles();
 
-// Also serve static files from Content/, Scripts/, and Uploads/ in project root
+// Also serve static files from Content/ and Scripts/ in project root
 var contentRootPath = builder.Environment.ContentRootPath;
 
 if (Directory.Exists(Path.Combine(contentRootPath, "Content")))
 {
     app.UseStaticFiles(new StaticFileOptions
     {
-        FileProvider = new PhysicalFileProvider(Path.Combine(contentRootPath, "Content")),
+        FileProvider = new Microsoft.Extensions.FileProviders.PhysicalFileProvider(Path.Combine(contentRootPath, "Content")),
         RequestPath = "/Content"
     });
 }
@@ -47,19 +62,13 @@ if (Directory.Exists(Path.Combine(contentRootPath, "Scripts")))
 {
     app.UseStaticFiles(new StaticFileOptions
     {
-        FileProvider = new PhysicalFileProvider(Path.Combine(contentRootPath, "Scripts")),
+        FileProvider = new Microsoft.Extensions.FileProviders.PhysicalFileProvider(Path.Combine(contentRootPath, "Scripts")),
         RequestPath = "/Scripts"
     });
 }
 
-if (Directory.Exists(Path.Combine(contentRootPath, "Uploads")))
-{
-    app.UseStaticFiles(new StaticFileOptions
-    {
-        FileProvider = new PhysicalFileProvider(Path.Combine(contentRootPath, "Uploads")),
-        RequestPath = "/Uploads"
-    });
-}
+// NOTE: The Uploads/TeachingMaterials/ static file middleware has been removed.
+// Teaching material images are now stored in Azure Blob Storage and served via their blob URLs.
 
 app.UseRouting();
 
diff --git a/ContosoUniversity/Views/Courses/Details.cshtml b/ContosoUniversity/Views/Courses/Details.cshtml
index a19bb69..fe0bf7d 100644
--- a/ContosoUniversity/Views/Courses/Details.cshtml
+++ b/ContosoUniversity/Views/Courses/Details.cshtml
@@ -40,7 +40,7 @@
         <dd>
             @if (!string.IsNullOrEmpty(Model.TeachingMaterialImagePath))
             {
-                <img src="@Url.Content(Model.TeachingMaterialImagePath)" alt="Teaching Material" style="max-width: 300px; max-height: 300px; border: 1px solid #ddd; padding: 5px;" />
+                <img src="@Model.TeachingMaterialImagePath" alt="Teaching Material" style="max-width: 300px; max-height: 300px; border: 1px solid #ddd; padding: 5px;" />
             }
             else
             {
diff --git a/ContosoUniversity/Views/Courses/Edit.cshtml b/ContosoUniversity/Views/Courses/Edit.cshtml
index 79a7d11..e238d86 100644
--- a/ContosoUniversity/Views/Courses/Edit.cshtml
+++ b/ContosoUniversity/Views/Courses/Edit.cshtml
@@ -55,7 +55,7 @@
                 {
                     <div class="current-image" style="margin-bottom: 10px;">
                         <p><strong>Current Image:</strong></p>
-                        <img src="@Url.Content(Model.TeachingMaterialImagePath)" alt="Teaching Material" style="max-width: 200px; max-height: 200px; border: 1px solid #ddd; padding: 5px;" />
+                        <img src="@Model.TeachingMaterialImagePath" alt="Teaching Material" style="max-width: 200px; max-height: 200px; border: 1px solid #ddd; padding: 5px;" />
                     </div>
                 }
                 <input type="file" name="teachingMaterialImage" accept="image/*" class="form-control" />
diff --git a/ContosoUniversity/Views/Courses/Index.cshtml b/ContosoUniversity/Views/Courses/Index.cshtml
index 5e2ae74..b189d47 100644
--- a/ContosoUniversity/Views/Courses/Index.cshtml
+++ b/ContosoUniversity/Views/Courses/Index.cshtml
@@ -47,7 +47,7 @@
         <td>
             @if (!string.IsNullOrEmpty(item.TeachingMaterialImagePath))
             {
-                <img src="@Url.Content(item.TeachingMaterialImagePath)" alt="Teaching Material" style="max-width: 50px; max-height: 50px; border: 1px solid #ddd;" />
+                <img src="@item.TeachingMaterialImagePath" alt="Teaching Material" style="max-width: 50px; max-height: 50px; border: 1px solid #ddd;" />
             }
             else
             {
diff --git a/ContosoUniversity/appsettings.json b/ContosoUniversity/appsettings.json
index dbde1e7..c131cca 100644
--- a/ContosoUniversity/appsettings.json
+++ b/ContosoUniversity/appsettings.json
@@ -5,6 +5,10 @@
   "AzureServiceBus": {
     "FullyQualifiedNamespace": "${SERVICE_BUS_NAMESPACE}.servicebus.windows.net"
   },
+  "Storage": {
+    "ServiceUri": "https://${STORAGE_ACCOUNT_NAME}.blob.core.windows.net",
+    "ContainerName": "teaching-materials"
+  },
   "Logging": {
     "LogLevel": {
       "Default": "Information",
